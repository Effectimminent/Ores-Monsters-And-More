1.0.N should be incremented every time the (src) code is altered in any way shape or form
1.N.0 should be incremented every time a minor change is complete eg: added new tools/armors AND AFTER ALL MAJOR BUGS HAVE BEEN FIXED
N.0.0  should be incremented every time there is a major change eg: changing mc version, partial or full rewrites of  a large portion of the codebase